import * as React from "react";
import * as ReactDOM from "react-dom";
import {PreviewCart} from '../components/previewCart';

const rootEl = document.getElementById('app-root');

ReactDOM.render(<PreviewCart/>, rootEl);

