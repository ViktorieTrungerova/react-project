import * as React from "react";
import * as ReactDOM from "react-dom";
import { Cart } from '../components/cart';
const rootEl = document.getElementById('app-root');
ReactDOM.render(React.createElement(Cart, null), rootEl);
